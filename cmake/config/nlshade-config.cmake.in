if(EXISTS ${CMAKE_INSTALL_FULL_LIBDIR}/cmake/${LIBRARY_COMPONENT_NAME}/${LIBRARY_COMPONENT_TARGETS}.cmake)
  include(${CMAKE_INSTALL_FULL_LIBDIR}/cmake/${LIBRARY_COMPONENT_NAME}/${LIBRARY_COMPONENT_TARGETS}.cmake)
  file(GLOB_RECURSE ${LIBRARY_COMPONENT_NAME}_HEADERS ${TARGET_HEADERS_LOCATION}/*.h ${TARGET_HEADERS_LOCATION}/*.hpp )
endif(EXISTS ${CMAKE_INSTALL_FULL_LIBDIR}/cmake/${LIBRARY_COMPONENT_NAME}/${LIBRARY_COMPONENT_TARGETS}.cmake)
